# Save public key

After administrator (person who has access to dbvisor-agent) provide public key you should save it locally using following command:

```bash
db-manager save-key
```

after that pass key and press Ctrl+D to save it.