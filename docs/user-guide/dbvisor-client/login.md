# Login

To log in to the service, use the following command:

```bash
db-manager login
```

After executing this command, you will be prompted to enter your email and password. If it's your first time logging in, you will be asked for a public key, which can be obtained from the dbvisor-agent (please consult your administrator for assistance).